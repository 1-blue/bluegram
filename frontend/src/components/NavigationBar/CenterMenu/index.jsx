import React from "react";

// components
import SearchInput from "@components/common/SearchInput";

const CenterMenu = () => {
  return <SearchInput type="search" placeholder="검색" />;
};

export default CenterMenu;
